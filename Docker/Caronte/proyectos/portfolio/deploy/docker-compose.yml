services:
  web:
    build: 
      context: .. #portfolio
      args: #variables en dockerfile para la construcción de la
        - USUARIO=maria
      dockerfile: ./deploy/Dockerfile
    container_name: ctportfolio
    ports:
      - "8810:80"

